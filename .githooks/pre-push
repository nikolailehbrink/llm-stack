#!/bin/sh
# Pre-push: thorough checks before sharing code

echo "Running typecheck..."
npm run typecheck || {
  echo "Type errors found."
  exit 1
}

echo "Running tests..."
npm run test || {
  echo "Tests failed."
  exit 1
}
